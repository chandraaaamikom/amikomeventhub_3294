@extends('layouts.admin')
@section('content')
<header class="flex items-center justify-between mb-8"><div><h1 class="text-3xl font-black">Kupon & Voucher</h1><p class="text-slate-500 mt-1">Kelola diskon untuk pembelian tiket.</p></div><a href="{{ route('admin.coupons.create') }}" class="rounded-2xl bg-indigo-600 px-5 py-3 font-bold text-white">+ Buat Kupon</a></header>
@if(session('success'))<div class="mb-6 rounded-2xl bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif
<div class="overflow-hidden rounded-3xl bg-white border border-slate-100 shadow-sm"><table class="w-full text-left"><thead class="bg-slate-50 text-xs uppercase text-slate-400"><tr><th class="p-5">Kode</th><th class="p-5">Diskon</th><th class="p-5">Periode</th><th class="p-5">Pemakaian</th><th class="p-5"></th></tr></thead><tbody class="divide-y">@forelse($coupons as $coupon)<tr><td class="p-5 font-black">{{ $coupon->code }} @if(! $coupon->is_active)<span class="ml-2 text-xs text-rose-500">Nonaktif</span>@endif</td><td class="p-5">{{ $coupon->type === 'percent' ? $coupon->value.'%' : 'Rp '.number_format($coupon->value,0,',','.') }}</td><td class="p-5 text-sm text-slate-500">{{ $coupon->starts_at?->format('d M Y') ?? 'Sekarang' }} - {{ $coupon->ends_at?->format('d M Y') ?? 'Tanpa batas' }}</td><td class="p-5">{{ $coupon->used_count }} / {{ $coupon->usage_limit ?? '∞' }}</td><td class="p-5 text-right"><a href="{{ route('admin.coupons.edit',$coupon) }}" class="font-bold text-indigo-600">Edit</a></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-500">Belum ada kupon.</td></tr>@endforelse</tbody></table></div>
@endsection
